import { useMemo, useState } from 'react'
import './App.css'

const assets = [
  { id: 1, type: 'AI MODEL', title: 'Drishti Vision 2.1', seller: 'Kaveri Labs · Bengaluru', description: 'Multimodal inference for industrial inspection.', price: 199000, rating: '4.9', safety: 'L3', color: 'coral', initials: 'DV' },
  { id: 2, type: 'SOFTWARE', title: 'LekhaOS', seller: 'Nivasa Systems · Mumbai', description: 'The open financial OS for modern Indian teams.', price: 725000, rating: '4.8', safety: 'L2', color: 'green', initials: 'LO' },
  { id: 3, type: 'IP / PATENT', title: 'Surya Mesh Array', seller: 'IIT Madras Research Park', description: 'Patent-ready hardware architecture for distributed energy.', price: 1180000, rating: '5.0', safety: 'L1', color: 'blue', initials: 'SM' },
  { id: 4, type: 'SAAS', title: 'Dabba Signal Room', seller: 'Fieldwork Studio · Pune', description: 'A private intelligence layer for distributed teams.', price: 350000, rating: '4.7', safety: 'L2', color: 'yellow', initials: 'DS' },
]

const formatINR = (amount) => `₹${amount.toLocaleString('en-IN')}`

const navItems = [['Overview', '⌂'], ['Marketplace', '⌕'], ['My Purchases', '◫'], ['Messages', '◌'], ['Notifications', '◍']]

function App() {
  const [page, setPage] = useState('Overview')
  const [query, setQuery] = useState('')
  const [saved, setSaved] = useState([])
  const [toast, setToast] = useState('')
  const [paymentAsset, setPaymentAsset] = useState(null)
  const filteredAssets = useMemo(() => assets.filter((asset) => `${asset.title} ${asset.type} ${asset.seller}`.toLowerCase().includes(query.toLowerCase())), [query])
  const notify = (message) => { setToast(message); window.setTimeout(() => setToast(''), 2600) }
  const toggleSave = (id) => setSaved((current) => current.includes(id) ? current.filter((item) => item !== id) : [...current, id])
  const go = (destination) => setPage(destination)

  return <div className="app-shell">
    <aside className="sidebar">
      <button className="brand" onClick={() => go('Overview')}><span className="brand-mark">V</span><span>VAULT</span></button>
      <div className="workspace-label">WORKSPACE <span>⌄</span></div>
      <nav className="side-nav">{navItems.map(([label, icon]) => <button key={label} className={page === label ? 'side-link active' : 'side-link'} onClick={() => go(label)}><span>{icon}</span>{label}{label === 'Notifications' && <b className="nav-count">3</b>}</button>)}</nav>
      <div className="side-rule"></div><div className="workspace-label">TRUST CENTER</div>
      <nav className="side-nav"><button className="side-link" onClick={() => go('PatentForge')}><span>◇</span>PatentForge</button><button className="side-link" onClick={() => go('AI Safety')}><span>◈</span>AI Safety</button><button className="side-link" onClick={() => go('Build Together')}><span>↗</span>Build Together</button></nav>
      <div className="sidebar-bottom"><div className="help-card"><span className="help-icon">?</span><strong>Need a hand?</strong><small>Visit the India trust center</small></div><button className="profile-mini" onClick={() => go('Profile')}><span className="avatar">RS</span><span><b>Riya Sharma</b><small>Buyer account · India</small></span><span>···</span></button></div>
    </aside>
    <main className="main-content">
      <header className="topbar"><div className="mobile-brand"><span className="brand-mark">V</span> VAULT</div><div className="breadcrumb"><span>Workspace</span><b>/</b><strong>{page}</strong></div><div className="top-actions"><button className="top-icon" onClick={() => go('Notifications')} aria-label="Notifications">◍<i>3</i></button><button className="top-avatar" onClick={() => go('Profile')}>AK</button></div></header>
      {page === 'Overview' && <Overview go={go} startPayment={setPaymentAsset} />}
      {page === 'Marketplace' && <Marketplace assets={filteredAssets} query={query} setQuery={setQuery} saved={saved} toggleSave={toggleSave} go={go} startPayment={setPaymentAsset} />}
      {page === 'My Purchases' && <Purchases go={go} />}
      {page === 'Messages' && <Messages notify={notify} />}
      {page === 'Notifications' && <Notifications />}
      {page === 'PatentForge' && <PatentForge />}
      {page === 'AI Safety' && <Safety />}
      {page === 'Build Together' && <BuildTogether />}
      {page === 'Profile' && <Profile notify={notify} />}
      {page === 'Sell' && <SellAsset notify={notify} />}
      {page === 'Transaction' && <Transaction />}
      {page === 'Admin' && <Admin />}
      {page === 'Support' && <Support notify={notify} />}
      {toast && <div className="toast"><span>✓</span>{toast}</div>}
      {paymentAsset && <PaymentModal asset={paymentAsset} onClose={() => setPaymentAsset(null)} onSuccess={() => { setPaymentAsset(null); notify('Payment secured in Vault escrow') }} />}
    </main>
  </div>
}

function PageTitle({ eyebrow, title, description, action, onAction }) { return <div className="page-title"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1>{description && <p className="page-description">{description}</p>}</div>{action && <button className="primary-button small" onClick={onAction}>{action} <span>↗</span></button>}</div> }

function Overview({ go, startPayment }) { return <div className="page"><PageTitle eyebrow="TUESDAY, SEPTEMBER 15, 2026 · IST" title={<>Good morning, Riya <span className="wave">✦</span></>} description="Your trusted workspace for India's digital ownership economy." action="Explore marketplace" onAction={() => go('Marketplace')} /><section className="overview-grid"><div className="spotlight panel"><div className="spotlight-copy"><span className="status-dot">● INDIA'S TRUST LAYER</span><h2>Acquire with<br /><em>confidence.</em></h2><p>From Bengaluru code to Chennai patents, every asset is evaluated through legal diligence, AI governance, and escrow-secured payment.</p><button className="light-button" onClick={() => go('PatentForge')}>Explore the trust layer <span>→</span></button></div><div className="spotlight-art"><div className="glow-ring"></div><div className="core-mark">V</div><span className="float-label one">LEGAL<br /><b>VERIFIED</b></span><span className="float-label two">ESCROW<br /><b>SECURED</b></span></div></div><div className="stat-stack"><div className="stat-card panel"><span className="stat-icon gold">₹</span><span className="stat-label">PORTFOLIO VALUE</span><strong>₹12,84,000</strong><small className="positive">↗ 12.4% this month</small></div><div className="stat-card panel"><span className="stat-icon blue">◫</span><span className="stat-label">ACTIVE TRANSACTIONS</span><strong>03</strong><small>1 needs your attention</small></div></div></section><section className="section-block"><div className="section-heading"><div><p className="eyebrow">CURATED FOR YOU · INDIA</p><h2>Worth a closer look.</h2></div><button className="text-button" onClick={() => go('Marketplace')}>View marketplace <span>→</span></button></div><div className="asset-grid">{assets.slice(0, 3).map((asset) => <AssetCard key={asset.id} asset={asset} onBuy={() => startPayment(asset)} />)}</div></section><section className="activity-panel panel"><div className="section-heading"><div><p className="eyebrow">RECENT ACTIVITY</p><h2>Keep an eye on it.</h2></div><button className="text-button" onClick={() => go('My Purchases')}>See all activity <span>→</span></button></div><div className="activity-row"><span className="activity-icon green-bg">✓</span><div><strong>LekhaOS ownership review complete</strong><small>2 hours ago · Transaction #VL-2841</small></div><span className="tag success">CLEARED</span></div><div className="activity-row"><span className="activity-icon purple-bg">◌</span><div><strong>New message from Kaveri Labs</strong><small>Yesterday · Drishti Vision 2.1</small></div><span className="tag">REPLY</span></div></section></div> }

function AssetCard({ asset, onBuy, saved, toggleSave }) { return <article className="asset-card panel"><div className={`asset-preview ${asset.color}`}><span className="asset-type">{asset.type}</span><button className="save-button" onClick={() => toggleSave?.(asset.id)} aria-label={`Save ${asset.title}`}>{saved?.includes(asset.id) ? '♥' : '♡'}</button><div className="preview-glyph">{asset.initials}</div><span className="preview-line"></span></div><div className="asset-info"><div className="asset-title-row"><h3>{asset.title}</h3><span className="verified">✓</span></div><p>{asset.description}</p><div className="asset-seller">by {asset.seller} <span>·</span> <span className="stars">★</span> {asset.rating}</div><div className="asset-footer"><span className="asset-price">{formatINR(asset.price)}</span><span className="safety-badge">AI {asset.safety}</span></div><div className="card-actions"><button className="outline-button" onClick={onBuy}>View details</button><button className="buy-button" onClick={onBuy}>Buy securely</button></div></div></article> }

function Marketplace({ assets: visibleAssets, query, setQuery, saved, toggleSave, go, startPayment }) { return <div className="page"><PageTitle eyebrow="THE MARKETPLACE · INDIA" title="Find what’s next." description="A considered collection of Indian software, intelligence, and ideas with a clear path to ownership." action="List an asset" onAction={() => go('Sell')} /><div className="market-tools"><div className="search-box"><span>⌕</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search products, models, patents..." aria-label="Search assets" /></div><div className="filter-group"><button className="filter active">All assets</button><button className="filter">AI models</button><button className="filter">Software</button><button className="filter">IP & patents</button><button className="filter">Ideas</button></div></div><div className="market-meta"><span>{visibleAssets.length * 27 + 3} assets available across India</span><button className="sort-button">Sort: <b>Most relevant</b>⌄</button></div><div className="asset-grid marketplace-grid">{visibleAssets.map((asset) => <AssetCard key={asset.id} asset={asset} saved={saved} toggleSave={toggleSave} onBuy={() => startPayment(asset)} />)}</div>{visibleAssets.length === 0 && <div className="empty-state panel"><span>⌕</span><h3>No assets found</h3><p>Try a different search term.</p></div>}</div> }

function Purchases({ go }) { return <div className="page"><PageTitle eyebrow="YOUR PORTFOLIO · INR" title="My purchases." description="A clear view of every asset you own and every transfer in motion." action="Explore more" onAction={() => go('Marketplace')} /><div className="metric-row"><div className="metric panel"><small>TOTAL PURCHASES</small><strong>08</strong><span>↗ 2 this quarter</span></div><div className="metric panel"><small>IN PROGRESS</small><strong>02</strong><span className="gold-text">Needs attention</span></div><div className="metric panel"><small>ESCROW BALANCE</small><strong>₹2,84,000</strong><span>Held securely in INR</span></div></div><div className="table-panel panel"><div className="section-heading"><h2>Active transactions</h2><span className="tag">2 IN PROGRESS</span></div><div className="transaction"><div className="table-icon coral">DV</div><div><strong>Drishti Vision 2.1</strong><small>Kaveri Labs · #VL-2841</small></div><div className="transaction-stage"><span className="stage-line"><i></i></span><small>Legal verification</small></div><b>₹1,99,000</b><button className="chevron" onClick={() => go('Transaction')}>→</button></div><div className="transaction"><div className="table-icon green">LO</div><div><strong>LekhaOS</strong><small>Nivasa Systems · #VL-2798</small></div><div className="transaction-stage"><span className="stage-line complete"><i></i></span><small>Buyer confirmation</small></div><b>₹7,25,000</b><button className="chevron" onClick={() => go('Transaction')}>→</button></div></div></div> }

function Messages({ notify }) { return <div className="page"><PageTitle eyebrow="YOUR INBOX · INDIA" title="Messages." description="Direct lines to the people behind the products." action="New message" onAction={() => notify('Select an asset to start a conversation.')} /><div className="messages-layout panel"><div className="conversation-list"><div className="search-box compact"><span>⌕</span><input placeholder="Search conversations" /></div>{['Kaveri Labs', 'Nivasa Systems', 'IIT Madras Research Park'].map((name, index) => <button className={index === 0 ? 'conversation active' : 'conversation'} key={name}><span className={`table-icon ${['coral', 'green', 'blue'][index]}`}>{name.slice(0, 2)}</span><span><strong>{name}</strong><small>{['Re: Drishti Vision 2.1', 'Ownership transfer complete', 'A question about the patent'][index]}</small></span><i>{index === 0 ? '2m' : '1d'}</i></button>)}</div><div className="message-window"><div className="message-head"><div><strong>Kaveri Labs</strong><small>Drishti Vision 2.1 · Online</small></div><button>···</button></div><div className="messages"><span className="date-divider">TODAY</span><div className="bubble received">Hi Riya, the updated safety report is ready for your review.<small>10:42 AM</small></div><div className="bubble sent">Thanks, I’ll take a look now. The legal review looks good on my end.<small>10:44 AM</small></div></div><div className="message-compose"><input placeholder="Write a message..." /><button onClick={() => notify('Message sent')}>↑</button></div></div></div></div> }

function Notifications() { return <div className="page"><PageTitle eyebrow="STAY IN THE LOOP" title="Notifications." description="The important moments, without the noise." /><div className="notification-list panel">{[['green-bg', 'Legal verification complete', 'Arbor Vision 2.1 passed its legal ownership review.', '2 hours ago'], ['gold-bg', 'Payment held in escrow', '$2,400 is secured for your active transaction.', 'Yesterday'], ['purple-bg', 'New message from Nami Research', '“We’ve attached the updated patent schedule.”', 'Yesterday'], ['blue-bg', 'Welcome to Vault', 'Your workspace is ready. Explore the marketplace.', 'Sep 12']].map(([color, title, text, time]) => <div className="notification-row" key={title}><span className={`activity-icon ${color}`}>✓</span><div><strong>{title}</strong><p>{text}</p><small>{time}</small></div><button>···</button></div>)}</div></div> }

function PatentForge() { const steps = ['Asset submission', 'Ownership verification', 'Legal due diligence', 'Agreement generation', 'Digital signing', 'IP transfer', 'Transfer confirmation']; return <div className="page"><PageTitle eyebrow="LEGAL TRANSFER PROTOCOL" title="PatentForge™" description="A transparent, legally-minded path from digital asset to defensible ownership." action="Start a transfer" onAction={() => {}} /><div className="forge-layout"><div className="forge-intro panel"><span className="forge-seal">V</span><p className="eyebrow">THE VAULT STANDARD</p><h2>Ownership you<br /><em>can stand behind.</em></h2><p>PatentForge turns the complexity of digital IP transfer into a clear, auditable sequence. Every step leaves a record. Every party knows what happens next.</p><span className="legal-note">◈ Built with legal practitioners, for the real world.</span></div><div className="steps-panel panel">{steps.map((step, index) => <div className={index < 3 ? 'forge-step done' : index === 3 ? 'forge-step current' : 'forge-step'} key={step}><span className="step-number">{index < 3 ? '✓' : `0${index + 1}`}</span><div><strong>{step}</strong><small>{index < 3 ? 'Complete' : index === 3 ? 'Awaiting your review' : 'Up next'}</small></div>{index < 6 && <span className="step-arrow">↓</span>}</div>)}</div></div></div> }

function Safety() { return <div className="page"><PageTitle eyebrow="RESPONSIBLE INTELLIGENCE" title="AI Safety Governance." description="A shared language for understanding risk before intelligence changes hands." action="Read the charter" onAction={() => {}} /><div className="safety-hero panel"><div><span className="eyebrow">YOUR GOVERNANCE PROFILE</span><h2>Clarity at every<br /><em>level of risk.</em></h2></div><div className="safety-score"><div className="score-ring"><strong>87</strong><small>/100</small></div><span>YOUR PORTFOLIO SCORE</span></div></div><div className="safety-levels">{[['01', 'Basic Review', 'For low-impact models and automation.', 'L1', 'green'], ['02', 'Standard Safety Review', 'Data, bias, and deployment assessment.', 'L2', 'blue'], ['03', 'Advanced Risk Assessment', 'Deep evaluation for consequential systems.', 'L3', 'gold'], ['04', 'High-Risk Governance', 'Human oversight and continuous monitoring.', 'L4', 'coral']].map(([number, title, desc, level, color]) => <div className="safety-level panel" key={level}><span className={`level-index ${color}`}>{number}</span><span className="safety-level-code">{level}</span><h3>{title}</h3><p>{desc}</p><div className="progress"><i style={{ width: `${Number(number) * 25}%` }}></i></div><small>Governance readiness <b>{Number(number) * 25}%</b></small></div>)}</div></div> }

function BuildTogether() { return <div className="page"><PageTitle eyebrow="BEYOND THE TRANSACTION" title="Build Together." description="The best products keep getting better. Continue working with the people who built yours." action="Find a partner" onAction={() => {}} /><div className="build-hero panel"><div className="build-shape">↗</div><div><span className="eyebrow">THE SELLER RETENTION MODEL</span><h2>Ownership is<br /><em>just the beginning.</em></h2><p>Turn a one-time transfer into an ongoing working relationship, with clear scope, fair pricing, and the original builder close at hand.</p></div></div><div className="service-grid">{[['✦', 'Feature development', 'Shape the next release with the original team.'], ['◫', 'Customization', 'Make the product fit your exact workflow.'], ['⌁', 'Technical support', 'Get answers from people who know the system.'], ['↻', 'Maintenance & updates', 'Keep your asset secure and current.'], ['◎', 'Consulting', 'Bring hard-won context into your next decision.']].map(([icon, title, desc]) => <div className="service-card panel" key={title}><span>{icon}</span><h3>{title}</h3><p>{desc}</p><button className="text-button">Learn more →</button></div>)}</div></div> }

function Profile({ notify }) { return <div className="page"><PageTitle eyebrow="YOUR ACCOUNT · INDIA" title="Profile & settings." description="Keep your identity, security, and preferences in one place." /><div className="profile-layout"><div className="settings-nav panel">{['Profile information', 'Security', 'Notifications', 'Payment information', 'Verification'].map((item, index) => <button className={index === 0 ? 'settings-link active' : 'settings-link'} key={item}>{item}<span>→</span></button>)}</div><div className="settings-form panel"><div className="form-heading"><div className="large-avatar">RS</div><div><h2>Profile information</h2><p>This is how you appear across Vault.</p></div><button className="outline-button">Change photo</button></div><div className="form-grid"><label>Full name<input defaultValue="Riya Sharma" /></label><label>Email address<input defaultValue="riya@northstar.in" /></label><label>Account type<input defaultValue="Buyer · Seller" readOnly /></label><label>Company<input defaultValue="Northstar Ventures India" /></label></div><button className="primary-button small" onClick={() => notify('Profile changes saved')}>Save changes <span>✓</span></button></div></div></div> }

function SellAsset({ notify }) { const [step, setStep] = useState(1); const steps = ['Basic information', 'Technical details', 'Legal ownership', 'AI safety', 'Files', 'Review']; return <div className="page"><PageTitle eyebrow="SELL ON VAULT" title="List your asset." description="Give your work the trust layer it deserves. You can save and return at any time." /><div className="sell-layout"><div className="step-nav panel">{steps.map((item, index) => <button key={item} className={step === index + 1 ? 'sell-step active' : step > index + 1 ? 'sell-step done' : 'sell-step'} onClick={() => setStep(index + 1)}><span>{step > index + 1 ? '✓' : `0${index + 1}`}</span>{item}</button>)}</div><div className="sell-form panel"><div className="form-heading"><div><p className="eyebrow">STEP 0{step} OF 06</p><h2>{steps[step - 1]}</h2><p>{step === 1 ? 'Start with the essentials buyers need to understand your work.' : 'Add the detail that makes a considered purchase possible.'}</p></div></div>{step === 1 && <div className="form-grid"><label>Product name<input placeholder="e.g. Arbor Vision 2.1" /></label><label>Category<select><option>AI model</option><option>Software</option><option>Patent</option><option>Digital product</option></select></label><label className="full-field">Description<textarea placeholder="What does this asset make possible?" /></label><label>Asking price<input placeholder="$ 0.00" /></label><label>Version<input placeholder="1.0.0" /></label></div>}{step === 2 && <div className="form-grid"><label>Technology stack<input placeholder="Python, PyTorch, React..." /></label><label>Demo URL<input placeholder="https://" /></label><label className="full-field">Requirements<textarea placeholder="Environment, dependencies, and access requirements" /></label></div>}{step === 3 && <div className="form-grid"><label className="full-field">Ownership declaration<textarea placeholder="Describe your ownership and rights to sell this asset." /></label><label>License type<select><option>Commercial license</option><option>MIT</option><option>Custom transfer</option></select></label><label>Patent status<select><option>Not applicable</option><option>Patent pending</option><option>Granted</option></select></label></div>}{step === 4 && <div className="form-grid"><label>Contains AI? <select><option>Yes</option><option>No</option></select></label><label>Risk level<select><option>Level 2 · Standard</option><option>Level 3 · Advanced</option><option>Level 4 · High risk</option></select></label><label className="full-field">Training data information<textarea placeholder="Source, consent, and provenance of training data" /></label></div>}{step === 5 && <div className="upload-zone"><span>↑</span><h3>Drop files here, or browse</h3><p>Documentation, source code, licenses, and supporting files.</p><button className="outline-button">Choose files</button></div>}{step === 6 && <div className="review-box"><span className="review-check">✓</span><h3>Ready for review</h3><p>Your asset will enter Vault’s verification queue. We’ll review ownership, documentation, and safety before it appears in the marketplace.</p></div>}<div className="form-actions">{step > 1 && <button className="outline-button" onClick={() => setStep(step - 1)}>← Back</button>}<button className="primary-button small" onClick={() => step < 6 ? setStep(step + 1) : notify('Asset submitted for verification')}>{step < 6 ? 'Continue' : 'Submit for review'} <span>→</span></button></div></div></div></div> }

function Transaction() { const stages = ['Purchase initiated', 'Payment received', 'Asset verification', 'Legal verification', 'IP transfer', 'Buyer confirmation', 'Payment released']; return <div className="page"><PageTitle eyebrow="ESCROW TRANSACTION · #VL-2841" title="Drishti Vision 2.1." description="A secure, auditable path to ownership. Your funds remain protected until every condition is met." action="Message seller" onAction={() => {}} /><div className="transaction-hero panel"><div><span className="status-dot">● IN PROGRESS</span><h2>₹1,99,000 <small>INR</small></h2><p>Held securely in Vault escrow</p></div><div className="transaction-parties"><span>BUYER <b>Riya Sharma</b></span><span>SELLER <b>Kaveri Labs</b></span><span>STARTED <b>12 Sep 2026</b></span></div></div><div className="timeline-panel panel"><div className="section-heading"><div><p className="eyebrow">TRANSFER TIMELINE</p><h2>One clear step at a time.</h2></div><span className="tag">4 OF 7 COMPLETE</span></div><div className="timeline">{stages.map((stage, index) => <div className={index < 4 ? 'timeline-step done' : index === 4 ? 'timeline-step current' : 'timeline-step'} key={stage}><span>{index < 4 ? '✓' : index + 1}</span><div><strong>{stage}</strong><small>{index < 4 ? 'Complete' : index === 4 ? 'In progress' : 'Pending'}</small></div></div>)}</div></div></div> }

function Admin() { return <div className="page"><PageTitle eyebrow="ADMINISTRATION" title="Trust operations." description="The human review layer behind a healthier digital marketplace." action="Review queue" onAction={() => {}} /><div className="metric-row"><div className="metric panel"><small>USERS</small><strong>12,481</strong><span>↗ 8.2% this month</span></div><div className="metric panel"><small>IN ESCROW</small><strong>$284k</strong><span>Across 193 transactions</span></div><div className="metric panel"><small>REVIEWS QUEUED</small><strong>24</strong><span className="gold-text">8 high priority</span></div></div><div className="admin-grid"><div className="table-panel panel"><div className="section-heading"><h2>Verification queue</h2><span className="tag">24 OPEN</span></div>{['Thermal Mesh Array', 'Signal Room', 'Civic Lens API', 'Scribe Dataset'].map((name, index) => <div className="admin-row" key={name}><span className={`table-icon ${['blue', 'yellow', 'coral', 'green'][index]}`}>{name.slice(0, 2)}</span><div><strong>{name}</strong><small>{['IP ownership', 'AI safety', 'Legal documents', 'Training data'][index]}</small></div><span className="tag">REVIEW</span><button className="chevron">→</button></div>)}</div><div className="panel admin-note"><p className="eyebrow">SYSTEM HEALTH</p><h2>Trust is<br /><em>the product.</em></h2><p>All critical operations are within SLA. There are no unresolved complaints requiring escalation.</p><div className="health-line"><span></span><b>98.7%</b></div><small>Verification SLA compliance</small></div></div></div> }

function Support({ notify }) { return <div className="page"><PageTitle eyebrow="HELP CENTER" title="How can we help?" description="Find an answer or start a conversation with our support team." action="New support ticket" onAction={() => notify('Support ticket form opened')} /><div className="support-grid"><div className="support-card panel"><span>?</span><h3>Start with the FAQ</h3><p>Quick answers about transactions, ownership, and safety reviews.</p><button className="text-button">Browse questions →</button></div><div className="support-card panel"><span>◌</span><h3>Talk to support</h3><p>Our team typically replies within one business day.</p><button className="text-button" onClick={() => notify('Support ticket form opened')}>Create a ticket →</button></div><div className="support-card panel"><span>◇</span><h3>Trust center</h3><p>Learn how Vault protects every transfer and every participant.</p><button className="text-button">Visit trust center →</button></div></div><div className="table-panel panel"><div className="section-heading"><h2>Your support requests</h2><span className="tag success">ALL CLEAR</span></div><div className="empty-state"><span>✓</span><h3>No open requests</h3><p>When you need us, we’re here.</p></div></div></div> }

function PaymentModal({ asset, onClose, onSuccess }) { const [method, setMethod] = useState('UPI'); const [processing, setProcessing] = useState(false); const [complete, setComplete] = useState(false); const pay = () => { setProcessing(true); window.setTimeout(() => { setProcessing(false); setComplete(true) }, 900) }; return <div className="modal-backdrop" role="dialog" aria-modal="true" aria-label="Vault dummy payment gateway"><div className="payment-modal panel"><button className="modal-close" onClick={onClose} aria-label="Close payment">×</button>{complete ? <div className="payment-success"><span>✓</span><p className="eyebrow">PAYMENT AUTHORISED</p><h2>Secured in escrow.</h2><p>Your {formatINR(asset.price)} payment for <b>{asset.title}</b> is being held safely. The seller will now begin the transfer.</p><button className="primary-button" onClick={onSuccess}>Back to marketplace <span>→</span></button></div> : <><p className="eyebrow">VAULT CHECKOUT · TEST MODE</p><h2>Complete your purchase.</h2><div className="payment-asset"><span className={`table-icon ${asset.color}`}>{asset.initials}</span><div><strong>{asset.title}</strong><small>{asset.seller}</small></div><b>{formatINR(asset.price)}</b></div><div className="payment-security"><span>◈</span><p><b>Escrow protected</b><br />Funds release only after legal and IP transfer checks.</p></div><div className="payment-methods"><span className="method-label">SELECT PAYMENT METHOD</span><div>{['UPI', 'Card', 'Net banking'].map((item) => <button className={method === item ? 'payment-method active' : 'payment-method'} key={item} onClick={() => setMethod(item)}>{item === 'UPI' ? '◉' : item === 'Card' ? '▣' : '⌁'} {item}</button>)}</div></div>{method === 'UPI' ? <label className="payment-input">UPI ID<input placeholder="riya@oksbi" defaultValue="riya@oksbi" /></label> : <label className="payment-input">{method === 'Card' ? 'Card number' : 'Bank'}<input placeholder={method === 'Card' ? '4242 4242 4242 4242' : 'Select your bank'} /></label>}<button className="primary-button pay-button" onClick={pay} disabled={processing}>{processing ? 'Authorising payment...' : `Pay ${formatINR(asset.price)}`} <span>→</span></button><small className="test-note">Demo gateway · No real money will be charged</small></>}</div></div> }

export default App
